# Дубль зависимостей для pip (если без uv).
# Точные версии берутся из pyproject.toml; здесь — нижние границы.
vkbottle>=4.7.0,<5.0.0
aiosqlite>=0.20.0
pydantic-settings>=2.2.0
loguru>=0.7.0
# Доверие к системному хранилищу сертификатов (антивирус/прокси с TLS-инспекцией)
truststore>=0.9.0

# Для запуска тестов:
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
